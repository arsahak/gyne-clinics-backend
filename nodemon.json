{
  "watch": ["src", "config", "routes", "controller", "modal"],
  "ext": "ts,json",
  "ignore": ["src/**/*.spec.ts", "node_modules"],
  "exec": "ts-node server.ts",
  "env": {
    "NODE_ENV": "development"
  }
}

